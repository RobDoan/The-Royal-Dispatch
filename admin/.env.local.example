# INTERNAL_API_URL overrides backend URL for SSR calls (default: http://localhost:8000)
# Browser calls use /api proxy (Next.js rewrites in dev, k8s ingress in prod)
# INTERNAL_API_URL=http://backend:8000
NEXT_PUBLIC_FRONTEND_URL=https://the-royal-dispatch.quybits.com