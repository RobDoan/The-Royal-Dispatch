apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: admin
  namespace: royal-dispatch
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`admin.royal.local`)
      kind: Rule
      services:
        - name: admin
          port: 3001
