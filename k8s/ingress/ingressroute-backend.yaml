apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: backend
  namespace: royal-dispatch
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`api.royal.local`)
      kind: Rule
      services:
        - name: backend
          port: 8000
