apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frontend
  namespace: royal-dispatch
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`royal.local`)
      kind: Rule
      services:
        - name: frontend
          port: 3000
