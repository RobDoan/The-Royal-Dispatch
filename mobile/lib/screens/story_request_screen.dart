import 'package:flutter/material.dart';

class StoryRequestScreen extends StatelessWidget {
  const StoryRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('StoryRequestScreen'));
  }
}
